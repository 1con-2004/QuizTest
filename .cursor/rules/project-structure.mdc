---
description: 
globs: 
alwaysApply: true
---
# QuizTest项目结构

本项目是一个使用Docker容器化的全栈应用，包含以下主要组件：

- 前端：React应用 (`frontend/`)
- 后端：Node.js服务 (`backend/`)
- 数据库：MySQL和MongoDB
- 缓存：Redis
- 反向代理：Nginx

## 主要配置文件

- Docker配置: [docker-compose.yml](mdc:docker-compose.yml)
- 环境变量: [.env](mdc:.env)

## 目录结构

- `backend/`: Node.js后端服务
  - `src/controllers/`: API控制器
  - `src/models/`: 数据模型
  - `src/routes/`: API路由定义
  - `src/services/`: 业务逻辑服务
  - `src/middlewares/`: 中间件

- `frontend/`: React前端应用
  - `src/components/`: React组件
  - `src/pages/`: 页面组件
  - `src/api/`: API调用
  - `src/context/`: React上下文
  - `src/hooks/`: 自定义钩子

- `nginx/`: Nginx配置
  - `conf/`: Nginx配置文件
  - `certs/`: SSL证书

- `docker/`: Docker相关文件和数据库初始化脚本
